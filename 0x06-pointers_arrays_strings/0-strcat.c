#include "main.h"
#include <string.h>

/**
 * _strcat - concatenates two strings
 * @dest: string 1
 * @src: string 2
 *
 * Return: string concatenated (dest)
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
