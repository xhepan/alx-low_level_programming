#include "main.h"
#include <string.h>

/**
 * _strlen - checks length of string
 * @s: string to be checked
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len);
}
