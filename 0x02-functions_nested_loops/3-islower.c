#include "main.h"
#include <ctype.c>

/**
 * _islower - checks if character is lowercase
 * @c: character to be chacked
 *
 * Return: 0 if true
 */

int _islower(int c)
{
	int i;

	i = islower(c);
	return (i);
}
